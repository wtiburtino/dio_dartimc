import 'package:calculadora_imc/model/pessoa.dart';

String classificacaoIMC(Pessoa pessoa) {
  return pessoa.classificacaoIMC();
}

String calcularIMC(Pessoa pessoa) {
  return pessoa.calcularIMC();
}
